//crear y modular funciones especificas antes de hacer la promesa
//ej funcion 'readFile'- 'transformar la ruta en absoluta" - 'comprobar que la ruta exista'

export const sumar = (n1, n2) => {
    return n1+2
}

const restar = (n1, n2) => {
    return n1 - n2
}

const validarN2 = (n2) => {
    return n2!==0 ? true  : false
}
const dividir = (n1, n2) => {
    validarN2(n2)
    return n1 /n2
}

const isAbsolute = (ruta) => {
fdsfsdfsfs
return 
}